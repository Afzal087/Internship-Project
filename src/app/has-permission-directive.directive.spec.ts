import { HasPermissionDirective } from './has-permission.directive';

describe('HasPermissionDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new HasPermissionDirective();
    expect(directive).toBeTruthy();
  });
});
